# Manual Follow-Up Live Write Tests - 2026-09-07

## Request and Boundary

User required live write reproduction before any corrections. Both concerns
previously came only from source review. No application fix was performed.

## Results

- MANUAL-INV-001 confirmed in browser: new client 9 NGN90 agreed rate,
  container 28 NGN100 charge, invoice preview NGN100 saved as NGN90 draft
  INV-202609-005 (12). No invoice issue, payment or external communication.
- MANUAL-GATE-001 confirmed via owner live API and browser Gate Log:
  fixture 29 main status transition rejects missing releases with 409 but
  Gate-In accepts 200; repeat Gate-In overwrites original timestamp.
  Fixture 30 accepts Empty Gate-In before loaded movement and loaded Gate-Out
  without Gate-In, each 200. Both final persisted records visible in Gate Log.
- Working guards: pending-verification browser buttons disabled; empty exit
  without empty entry rejected 409. These do not close backend gaps.
- Historical NGN500 exception not changed or retested by another payment.

## Retained Data and Side Effects

All new records are labelled E2E-20260907 in E2E Lagos branch 2. Containers:
28 E2EI260907 pending verification; 29 E2EG260907A gate_in; 30 E2EG260907B
pending verification with intentionally inconsistent movement timestamps.
Client 9 and draft invoice 12 retained. Do not duplicate or silently delete.
Dashboard containers increased 9 to 12 and Terminal 0 to 1; budgeted charges
increased NGN100. Invoiced/collected/AR totals and shown bank balances unchanged.
Gate notifications/audit records were generated by normal API handlers.

## Tooling and Handoff

Added guarded one-off scripts/live-gate-followup-probe.py, not part of normal
build/test execution. Existing names prevent repeat creation. Uses prompted
credentials and standard library HTTP/cookies; no credentials saved.
An initial CSRF omission failed before creation; corrected normal-CSRF run
completed and logged out. Owner browser session restored and Gate Log checked.
No schema, app logic, deployment settings or PDF changed. Registers updated.

Next: await authorization to fix confirmed gate controls then invoice preview;
add isolated regression coverage and verify corrections without rewriting
existing genuine event times or issued invoice amounts. Existing audit closures
remain closed. Full evidence and timestamps are in LIVE_E2E_TEST_REGISTER.md.
